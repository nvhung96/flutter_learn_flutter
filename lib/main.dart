import 'package:flutter/material.dart';
import 'package:flutter_learn_flutter/ui/page/page_login_screen.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: PageScreen(),
    );
  }
}
